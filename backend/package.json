{
  "name": "honor-chess-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute honor-chess-db --file=./src/db/schema.sql",
    "db:seed": "wrangler d1 execute honor-chess-db --file=./src/db/seed.sql"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "wrangler": "^3.60.0",
    "typescript": "^5.5.0"
  }
}
